<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
                             http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <!--
        Root aggregator POM.

        This POM does three things:
          1. Declares the canonical groupId + version used by every module.
          2. Manages all dependency versions in one place (<dependencyManagement>).
          3. Lists the build order (<modules>) — Maven resolves inter-module
             dependencies automatically.

        It does NOT produce a JAR. packaging=pom.

        To build everything:   mvn clean package -DskipTests
        To build one module:   mvn -pl hammingstore-client clean package
        To run all tests:      mvn test
    -->

    <groupId>io.hammingstore</groupId>
    <artifactId>hammingstore-parent</artifactId>
    <version>1.0.0-SNAPSHOT</version>
    <packaging>pom</packaging>
    <name>HammingStore :: Parent</name>
    <description>
        Zero-GC neuro-symbolic reasoning engine for binary hypervectors.
        32x memory compression over float32. Sub-5ms multi-hop chain traversal.
    </description>
    <url>https://hammingstore.fly.dev</url>

    <licenses>
        <license>
            <name>MIT License</name>
            <url>https://opensource.org/licenses/MIT</url>
        </license>
    </licenses>

    <!-- =========================================================== -->
    <!-- Module build order — Maven resolves dependencies within this -->
    <!-- =========================================================== -->
    <modules>
        <module>hammingstore-core</module>
        <module>hammingstore-client</module>
    </modules>

    <!-- =========================================================== -->
    <!-- Global compiler + encoding settings                          -->
    <!-- =========================================================== -->
    <properties>
        <maven.compiler.release>22</maven.compiler.release>
        <maven.compiler.source>22</maven.compiler.source>
        <maven.compiler.target>22</maven.compiler.target>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>

        <!-- Dependency versions — single source of truth -->
        <grpc.version>1.61.0</grpc.version>
        <protobuf.version>3.25.2</protobuf.version>
        <junit.version>5.10.2</junit.version>
        <reactor.version>3.6.4</reactor.version>
        <mockito.version>5.10.0</mockito.version>
    </properties>

    <!-- =========================================================== -->
    <!-- Dependency version management — child modules inherit these  -->
    <!-- without specifying versions themselves                       -->
    <!-- =========================================================== -->
    <dependencyManagement>
        <dependencies>

            <!-- gRPC -->
            <dependency>
                <groupId>io.grpc</groupId>
                <artifactId>grpc-netty-shaded</artifactId>
                <version>${grpc.version}</version>
            </dependency>
            <dependency>
                <groupId>io.grpc</groupId>
                <artifactId>grpc-protobuf</artifactId>
                <version>${grpc.version}</version>
            </dependency>
            <dependency>
                <groupId>io.grpc</groupId>
                <artifactId>grpc-stub</artifactId>
                <version>${grpc.version}</version>
            </dependency>

            <!-- Protobuf -->
            <dependency>
                <groupId>com.google.protobuf</groupId>
                <artifactId>protobuf-java</artifactId>
                <version>${protobuf.version}</version>
            </dependency>

            <!-- Annotation API (required by gRPC generated stubs) -->
            <dependency>
                <groupId>javax.annotation</groupId>
                <artifactId>javax.annotation-api</artifactId>
                <version>1.3.2</version>
            </dependency>

            <!-- Project Reactor (optional reactive support in client) -->
            <dependency>
                <groupId>io.projectreactor</groupId>
                <artifactId>reactor-core</artifactId>
                <version>${reactor.version}</version>
                <optional>true</optional>
            </dependency>

            <!-- Internal sibling module — client depends on core's stubs -->
            <dependency>
                <groupId>io.hammingstore</groupId>
                <artifactId>hammingstore-core</artifactId>
                <version>${project.version}</version>
            </dependency>

            <!-- Test -->
            <dependency>
                <groupId>org.junit.jupiter</groupId>
                <artifactId>junit-jupiter</artifactId>
                <version>${junit.version}</version>
                <scope>test</scope>
            </dependency>
            <dependency>
                <groupId>org.mockito</groupId>
                <artifactId>mockito-core</artifactId>
                <version>${mockito.version}</version>
                <scope>test</scope>
            </dependency>

        </dependencies>
    </dependencyManagement>

    <!-- =========================================================== -->
    <!-- Plugin management — versions locked once here               -->
    <!-- =========================================================== -->
    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <version>3.13.0</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-surefire-plugin</artifactId>
                    <version>3.2.5</version>
                    <configuration>
                        <argLine>--add-opens java.base/java.lang=ALL-UNNAMED --add-modules jdk.random</argLine>
                    </configuration>
                </plugin>
                <plugin>
                    <groupId>com.github.os72</groupId>
                    <artifactId>protoc-jar-maven-plugin</artifactId>
                    <version>3.11.4</version>
                </plugin>
                <plugin>
                    <groupId>org.codehaus.mojo</groupId>
                    <artifactId>build-helper-maven-plugin</artifactId>
                    <version>3.5.0</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-shade-plugin</artifactId>
                    <version>3.3.0</version>
                </plugin>
            </plugins>
        </pluginManagement>
    </build>

</project>